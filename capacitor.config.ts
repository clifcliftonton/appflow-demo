import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.central1.app',
  appName: 'central1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
